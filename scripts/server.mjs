import { createServer } from 'node:http'; import { readFile, stat } from 'node:fs/promises'; import { join, extname } from 'node:path'; import { fileURLToPath } from 'node:url';
const root=join(fileURLToPath(new URL('..',import.meta.url))); const mime={'.html':'text/html','.js':'text/javascript','.css':'text/css','.svg':'image/svg+xml','.jpg':'image/jpeg','.jpeg':'image/jpeg','.png':'image/png','.webp':'image/webp'};
createServer(async(req,res)=>{try{let p=decodeURIComponent(req.url.split('?')[0]); if(p==='/' )p='/index.html'; const f=join(root,p); const s=await stat(f); if(!s.isFile())throw 0; res.writeHead(200,{'Content-Type':mime[extname(f)]||'text/plain'}); res.end(await readFile(f));}catch{res.writeHead(404);res.end('Not found')}}).listen(3006,()=>console.log('Patricia preview: http://localhost:3006'));
